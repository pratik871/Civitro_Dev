import type { Metadata } from "next";
import { Inter, JetBrains_Mono } from "next/font/google";
import { Providers } from "./providers";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-mono",
  display: "swap",
});

export const metadata: Metadata = {
  title: {
    default: "Civitro - Democracy you can see",
    template: "%s | Civitro",
  },
  description:
    "Civitro is a Citizen-Government Collaboration Platform empowering Indian citizens to track issues, rate leaders, and participate in civic governance.",
  keywords: [
    "civic engagement",
    "government",
    "India",
    "democracy",
    "transparency",
    "issues",
    "leaders",
  ],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${inter.variable} ${jetbrainsMono.variable}`}>
      <body className="font-sans">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
